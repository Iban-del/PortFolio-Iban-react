import type { linkParameters } from "./Link"


export interface navBarContent{
    buttons:linkParameters
}

/**
 * composant NavBar
 */
const NavBar = ({}:navBarContent) =>{

    return <nav>
        
    </nav>
}

export default NavBar