

interface buttonParameters {
    text:string
    onClick?:()=>void,
    clas?:string,
    
}

const Button = ({
    text,
    onClick,
    clas
}:buttonParameters) => {

    return <button>

    </button>
} 

export default Button