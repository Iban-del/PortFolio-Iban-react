

interface buttonParameters {
    text:string
    onClick?:()=>void,
    class?:string,
    
}

const Button = ({
    text,
    onClick,
    class

}:buttonParameters) => {

    return <button>

    </button>
} 

export default Button